# Provenance snapshots

The setup process copies selected immutable project records here, including the original v8 manifest and freeze-v3 reports.

These files document the research corpus from which the public publication copy was generated.

They are not the public data manifest. The public manifest is `data/manifest.json`.
